{
  "name": "browser-automation",
  "version": "0.1.3",
  "description": "Browser Automation Desktop App with Voice Interface",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "server": "python server.py",
    "server:verbose": "python server.py --verbose",
    "server:debug": "python server.py --verbose --debug",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
